import React from "react";
import styled from "styled-components/macro";

const Title = styled.h1`
  font-size: 1.5em;
  text-align: center;
`;

function Header() {
  return <Title>Best filters in the business</Title>;
}
export default Header;
